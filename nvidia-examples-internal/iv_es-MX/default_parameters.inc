#!/bin/bash

WORKSPACE=${WORKSPACE:-"/workspace/"}

MODEL_NAME=${MODEL_NAME:-"es-MX"}
MODEL_PATH=${MODEL_PATH:-"${WORKSPACE}/models/iv/${MODEL_NAME}/"}

DATASET=${DATASET:-"${WORKSPACE}/datasets/iv/${MODEL_NAME}/"}

GPU=${GPU:-0}
GPU_FEATURE=${GPU_FEATURE:-0}
GPU_THREADS=${GPU_THREADS:-2}
COPY_THREADS=${COPY_THREADS:-3}

BEAM=${BEAM:-17}
LATTICE_BEAM=${LATTICE_BEAM:-2.5}
MAX_ACTIVE=${MAX_ACTIVE:-8000}
MAIN_Q_CAPACITY=${MAIN_Q_CAPACITY:-36000}
AUX_Q_CAPACITY=${AUX_Q_CAPACITY:-200000}
  
MAX_BATCH_SIZE=${MAX_BATCH_SIZE:-70}
BATCH_DRAIN_SIZE=${BATCH_DRAIN_SIZE:-5}
source best_params.inc

