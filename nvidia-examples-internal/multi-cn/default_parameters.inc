#!/bin/bash

ITERATIONS=${ITERATIONS:-10}

WORKSPACE=${WORKSPACE:-"/workspace/"}

MODEL_NAME=${MODEL_NAME:-"multi-cn"}
MODEL_PATH=${MODEL_PATH:-"${WORKSPACE}/models/${MODEL_NAME}/"}

DATASET=${DATASET:-"${WORKSPACE}/datasets/${MODEL_NAME}/"}

GPU=${GPU:-0}
GPU_THREADS=${GPU_THREADS:-2}
COPY_THREADS=${COPY_THREADS:-2}
BEAM=${BEAM:-12}
LATTICE_BEAM=${LATTICE_BEAM:-7}

MAX_ACTIVE=${MAX_ACTIVE:-7000}
MAIN_Q_CAPACITY=${MAIN_Q_CAPACITY:-50000}
AUX_Q_CAPACITY=${AUX_Q_CAPACITY:-100000}
  
MAX_BATCH_SIZE=${MAX_BATCH_SIZE:-100}
BATCH_DRAIN_SIZE=${BATCH_DRAIN_SIZE:-10}
